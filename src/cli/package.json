{
  "name": "cli",
  "version": "1.0.0",
  "description": "CLI helper that parses commands and executes the given runnable tied to that command",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Bechtel",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "logger": "../logger",
    "path": "^0.12.7",
    "readline": "^1.3.0"
  }
}
